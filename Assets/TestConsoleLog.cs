using UnityEngine;

public class TestConsoleLog : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Test Console Log");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
